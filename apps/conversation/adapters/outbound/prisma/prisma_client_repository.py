"""Outbound Prisma repository for `conversation`.

Skeleton only — implements the app's outbound ports with the shared Prisma
client (`config.db`) in a later change. Row → DTO mapping stays here.
"""


class PrismaConversationRepository:
    """TODO: implement the app's outbound ports using the Prisma client."""
